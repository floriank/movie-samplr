<div class="list">
  <h2>
    <%= list.name %>
    <small class="actions">
      <%= link_to t('.rename'), edit_list_path(list), class: 'rename-link' %>
      <% unless list.default? %>
        | <%= link_to t('.delete'), '#' %>
      <% end %>
    </small>
  </h2>
</div>
